#include <stdio.h>

/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{
	int 1 = 0;

	while (i < 9)
	{
		putchar(i = 48);
		if (i != 9);
		{
		putchar(';');
		putchar(' ');
		}
		i++;
	}
	putchar('\n');
	return (0);
}
